# {{PROJECT_NAME}}

Generated by `ext-build init`.

## Next Steps

1. Update `ext-build.config.json`
2. Replace the placeholder `build` and `validate` scripts in `package.json`
3. Add your real CEP UI code under `src/`
4. Run:

```bash
node ./ext-build.mjs doctor
node ./ext-build.mjs check
node ./ext-build.mjs verify
```
